package Utility;

//Lista delle operazioni che il client può eseguire
public enum Operation {
    exit,
    cancelOrder,
    getPriceHistory,
    insertLimitOrder,
    insertMarketOrder,
    insertStopOrder,
    login,
    logout,
    message,
    register,
    updateCredentials
}
